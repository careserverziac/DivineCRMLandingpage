
@{
    ViewBag.Title = "SignUp";
    Layout = "~/Views/Shared/_LayoutMain.cshtml";
}

@*<h2>SignUp</h2>*@



<div class="mesh-bg">
    <div class="mesh-orb orb1"></div>
    <div class="mesh-orb orb2"></div>
    <div class="mesh-orb orb3"></div>
</div>
<div class="auth-page">
    <div class="auth-card">
        <div class="signup-logo">
            <img src="~/Content/Images/logo_only_light (1).svg" alt="Logo Icon">
            <img src="~/Content/Images/logo_name_only_light.svg" alt="Logo Text">
        </div>
        @*<span class="auth-logo">✦ DivineCRM</span>*@
        <p class="auth-logo-sub">Smart CRM for Growing Sales Teams</p>
        <h1 class="auth-title">Create Your <span class="grad-text">Account</span></h1>
        <p class="auth-sub">Join businesses growing with DivineCRM</p>
        <form class="auth-form" id="signupForm" novalidate>
            <!-- Full Name -->
            <div class="field-group">
                <label class="field-label" for="fullname">Full Name</label>
                <div class="field-wrap">
                    @*<span class="field-icon">👤</span>*@
                    <span class="field-icon"><i class="fa-solid fa-user"></i></span>
                    <input class="auth-input" type="text" id="fullname" name="fullname" placeholder="Enter Your Full Name" autocomplete="name" />
                </div>
                <div class="field-msg" id="name-msg"></div>
            </div>
            <!-- Phone -->
            <div class="field-group">
                <label class="field-label" for="phone">Phone Number</label>
                <div class="field-wrap">
                    @*<span class="field-icon">📱</span>*@
                    <span class="field-icon"><i class="fa-solid fa-phone"></i></span>
                    <input class="auth-input" type="tel" id="phone" name="phone" placeholder="Enter your Phone number" autocomplete="tel" />
                </div>
                <div class="field-msg" id="phone-msg"></div>
            </div>
            <!-- Email -->
            <div class="field-group">
                <label class="field-label" for="email">Email Address</label>
                <div class="field-wrap">
                    @*<span class="field-icon">✉️</span>*@
                    <span class="field-icon"><i class="fa-solid fa-envelope"></i></span>
                    <input class="auth-input" type="email" id="email" name="email" placeholder="Enter Your Email" autocomplete="email" />
                </div>
                <div class="field-msg" id="email-msg"></div>
            </div>
            <!-- Password -->
            <div class="field-group">
                <label class="field-label" for="password">Password</label>
                <div class="field-wrap">
                    @*<span class="field-icon">🔒</span>*@
                    <span class="field-icon"><i class="fa-solid fa-lock"></i></span>
                    <input class="auth-input" type="password" id="password" name="password" placeholder="Enter your password" autocomplete="new-password" style="padding-right: 42px;" />
                    <button type="button" class="pwd-toggle" id="pwdToggle" aria-label="Toggle password">👁️</button>
                </div>
                <div class="field-msg" id="password-msg"></div>
                <div class="strength-wrap">
                    <div class="strength-bar-bg"><div class="strength-bar" id="strengthBar"></div></div>
                    <div class="strength-label" id="strengthLabel"></div>
                </div>
            </div>
            <p class="terms-text">By signing up you agree to our <a href="#">Terms of Service</a> and <a href="#">Privacy Policy</a></p>
            <button type="submit" class="btn-submit" id="submitBtn">
                Create Free Account →
            </button>
        </form>
        <div class="auth-divider" style="margin-top: 20px;"><span>Already have an account?</span></div>
        <div class="auth-footer" style="margin-top: 12px;">
            <a href="/Home/Login">Sign in to DivineCRM →</a>
        </div>
    </div>
</div>
@*<script>// Password toggle
    const pwdInput = document.getElementById('password');
    document.getElementById('pwdToggle').addEventListener('click', function () {
        const show = pwdInput.type === 'password';
        pwdInput.type = show ? 'text' : 'password';
        this.textContent = show ? '🙈' : '👁️';
    });

    // Password strength
    document.getElementById('password').addEventListener('input', function () {
        const val = this.value;
        const bar = document.getElementById('strengthBar');
        const lbl = document.getElementById('strengthLabel');
        let score = 0;
        if (val.length >= 8) score++;
        if (/[A-Z]/.test(val)) score++;
        if (/[0-9]/.test(val)) score++;
        if (/[^A-Za-z0-9]/.test(val)) score++;
        const levels = [
            { w: '0%', bg: 'transparent', text: '' },
            { w: '25%', bg: '#ef4444', text: 'Weak' },
            { w: '50%', bg: '#f59e0b', text: 'Fair' },
            { w: '75%', bg: '#3b82f6', text: 'Good' },
            { w: '100%', bg: '#10b981', text: 'Strong' },
        ];
        const l = levels[score];
        bar.style.width = l.w;
        bar.style.background = l.bg;
        lbl.textContent = l.text;
        lbl.style.color = l.bg;
    });

    // Inline validation
    function showMsg(id, text, type) {
        const el = document.getElementById(id);
        el.textContent = text;
        el.className = 'field-msg show ' + type;
    }
    function clearMsg(id) {
        const el = document.getElementById(id);
        el.className = 'field-msg';
    }

    document.getElementById('fullname').addEventListener('blur', function () {
        if (this.value.trim().length < 3) {
            this.classList.remove('valid'); this.classList.add('invalid');
            showMsg('name-msg', 'Please enter your full name', 'err');
        } else {
            this.classList.remove('invalid'); this.classList.add('valid');
            clearMsg('name-msg');
        }
    });

    document.getElementById('phone').addEventListener('blur', function () {
        const ph = this.value.replace(/\s/g, '');
        if (!/^\+?[0-9]{8,15}$/.test(ph)) {
            this.classList.remove('valid'); this.classList.add('invalid');
            showMsg('phone-msg', 'Enter a valid phone number', 'err');
        } else {
            this.classList.remove('invalid'); this.classList.add('valid');
            clearMsg('phone-msg');
        }
    });

    document.getElementById('email').addEventListener('blur', function () {
        if (!/^[^\s@@]+@@[^\s@@]+\.[^\s@@]+$/.test(this.value)) {
            this.classList.remove('valid'); this.classList.add('invalid');
            showMsg('email-msg', 'Enter a valid email address', 'err');
        } else {
            this.classList.remove('invalid'); this.classList.add('valid');
            showMsg('email-msg', 'Looks good!', 'ok');
        }
    });

    // Submit
    //document.getElementById('signupForm').addEventListener('submit', function (e) {
    //    e.preventDefault();
    //    const btn = document.getElementById('submitBtn');
    //    btn.textContent = 'Creating account…';
    //    btn.style.opacity = '0.8';
    //    // Hook up your controller action here
    //    // this.submit();
    //    setTimeout(() => {
    //        btn.textContent = '✓ Account Created!';
    //        btn.style.background = 'linear-gradient(135deg,#10b981,#059669)';
    //    }, 1400);
    //});

    document.getElementById('signupForm').addEventListener('submit', function (e) {
    e.preventDefault();

    let isValid = true;

    const name = document.getElementById('fullname');
    const phone = document.getElementById('phone');
    const email = document.getElementById('email');
    const password = document.getElementById('password');

    // Name validation
    if (name.value.trim().length < 3) {
        name.classList.add('invalid');
        showMsg('name-msg', 'Please enter your full name', 'err');
        isValid = false;
    }

    // Phone validation
    const ph = phone.value.replace(/\s/g, '');
    if (!/^\+?[0-9]{8,15}$/.test(ph)) {
        phone.classList.add('invalid');
        showMsg('phone-msg', 'Enter a valid phone number', 'err');
        isValid = false;
    }

    // Email validation
    if (!/^[^\s@@]+@@[^\s@@]+\.[^\s@@]+$/.test(email.value)) {
        email.classList.add('invalid');
        showMsg('email-msg', 'Enter a valid email address', 'err');
        isValid = false;
    }

    // Password validation
    if (password.value.length < 8) {
        password.classList.add('invalid');
        showMsg('email-msg', 'Password must be at least 8 characters', 'err');
        isValid = false;
    }

    // ❌ Stop if invalid
    if (!isValid) return;

    // ✅ Continue only if valid
    const btn = document.getElementById('submitBtn');
    btn.textContent = 'Creating account…';
    btn.style.opacity = '0.8';

    setTimeout(() => {
        btn.textContent = '✓ Account Created!';
        btn.style.background = 'linear-gradient(135deg,#10b981,#059669)';
    }, 1400);
});
</script>*@
<script>
// Password toggle
const pwdInput = document.getElementById('password');
document.getElementById('pwdToggle').addEventListener('click', function () {
    const show = pwdInput.type === 'password';
    pwdInput.type = show ? 'text' : 'password';
    this.textContent = show ? '🙈' : '👁️';
});

// Password strength
pwdInput.addEventListener('input', function () {
    const val = this.value;
    const bar = document.getElementById('strengthBar');
    const lbl = document.getElementById('strengthLabel');

    let score = 0;
    if (val.length >= 8) score++;
    if (/[A-Z]/.test(val)) score++;
    if (/[0-9]/.test(val)) score++;
    if (/[^A-Za-z0-9]/.test(val)) score++;

    const levels = [
        { w: '0%', bg: 'transparent', text: '' },
        { w: '25%', bg: '#ef4444', text: 'Weak' },
        { w: '50%', bg: '#f59e0b', text: 'Fair' },
        { w: '75%', bg: '#3b82f6', text: 'Good' },
        { w: '100%', bg: '#10b981', text: 'Strong' },
    ];

    const l = levels[score];
    bar.style.width = l.w;
    bar.style.background = l.bg;
    lbl.textContent = l.text;
    lbl.style.color = l.bg;
});

// Message helpers
function showMsg(id, text, type) {
    const el = document.getElementById(id);
    el.textContent = text;
    el.className = 'field-msg show ' + type;
}

function clearMsg(id) {
    const el = document.getElementById(id);
    el.textContent = '';
    el.className = 'field-msg';
}

// Full name validation
document.getElementById('fullname').addEventListener('blur', function () {
    if (this.value.trim().length < 3) {
        this.classList.remove('valid');
        this.classList.add('invalid');
        showMsg('name-msg', 'Please enter your full name', 'err');
    } else {
        this.classList.remove('invalid');
        this.classList.add('valid');
        clearMsg('name-msg');
    }
});

// Phone validation
document.getElementById('phone').addEventListener('blur', function () {
    const ph = this.value.replace(/\s/g, '');
    if (!/^\+?[0-9]{8,15}$/.test(ph)) {
        this.classList.remove('valid');
        this.classList.add('invalid');
        showMsg('phone-msg', 'Enter a valid phone number', 'err');
    } else {
        this.classList.remove('invalid');
        this.classList.add('valid');
        clearMsg('phone-msg');
    }
});

// Email validation (FIXED REGEX)
document.getElementById('email').addEventListener('blur', function () {
    if (!/^[^\s@@]+@@[^\s@@]+\.[^\s@@]+$/.test(this.value)) {
        this.classList.remove('valid');
        this.classList.add('invalid');
        showMsg('email-msg', 'Enter a valid email address', 'err');
    } else {
        this.classList.remove('invalid');
        this.classList.add('valid');
        showMsg('email-msg', 'Looks good!', 'ok');
    }
});

// Password validation (on blur)
document.getElementById('password').addEventListener('blur', function () {
    if (this.value.length < 8) {
        this.classList.remove('valid');
        this.classList.add('invalid');
        showMsg('password-msg', 'Password must be at least 8 characters', 'err');
    } else {
        this.classList.remove('invalid');
        this.classList.add('valid');
        clearMsg('password-msg');
    }
});

// Submit validation
document.getElementById('signupForm').addEventListener('submit', function (e) {
    e.preventDefault();

    let isValid = true;

    const name = document.getElementById('fullname');
    const phone = document.getElementById('phone');
    const email = document.getElementById('email');
    const password = document.getElementById('password');

    // Name
    if (name.value.trim().length < 3) {
        name.classList.add('invalid');
        showMsg('name-msg', 'Please enter your full name', 'err');
        isValid = false;
    }

    // Phone
    const ph = phone.value.replace(/\s/g, '');
    if (!/^\+?[0-9]{8,15}$/.test(ph)) {
        phone.classList.add('invalid');
        showMsg('phone-msg', 'Enter a valid phone number', 'err');
        isValid = false;
    }

    // Email (FIXED)
    if (!/^[^\s@@]+@@[^\s@@]+\.[^\s@@]+$/.test(email.value)) {
        email.classList.add('invalid');
        showMsg('email-msg', 'Enter a valid email address', 'err');
        isValid = false;
    }

    // Password (FIXED MESSAGE TARGET)
    if (password.value.length < 8) {
        password.classList.add('invalid');
        showMsg('password-msg', 'Password must be at least 8 characters', 'err');
        isValid = false;
    }

    // Stop if invalid
    if (!isValid) return;

    // Success state
    const btn = document.getElementById('submitBtn');
    btn.textContent = 'Creating account…';
    btn.style.opacity = '0.8';

    setTimeout(() => {
        btn.textContent = '✓ Account Created!';
        btn.style.background = 'linear-gradient(135deg,#10b981,#059669)';
    }, 1400);
});
</script>